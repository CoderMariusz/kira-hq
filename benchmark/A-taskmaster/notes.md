# Benchmark A (Taskmaster parse-prd) — Run Notes

Date: 2026-04-16
Operator: Claude Code subagent (Approach A)

## Environment

- task-master version: 0.43.1
- Provider: claude-code (no API key required)
  - main: sonnet
  - research: opus
  - fallback: sonnet
- All invocations wrapped with `env -u CLAUDECODE -u CLAUDE_CODE_ENTRYPOINT -u CLAUDE_CODE_EXECPATH -u ANTHROPIC_API_KEY` to avoid claude-agent-sdk RangeError.

## Commands executed (exit codes)

| Step | Command | Exit |
|------|---------|------|
| 1 | `task-master init -y --no-git --no-aliases --skip-install` | 0 |
| 2 | `task-master models --set-main sonnet --claude-code` | 0 |
| 3 | `task-master models --set-research opus --claude-code` | 0 |
| 4 | `task-master models --set-fallback sonnet --claude-code` | 0 |
| 5 | `task-master parse-prd <prd> --num-tasks 25 --force` | 0 |
| 6 | `task-master list --json > tasks.json` | 0 |
| 7 | `task-master list > tasks-list.txt` | 0 |

## parse-prd wall time

392 seconds (≈6m32s).

## Token usage (reported by parse-prd telemetry)

- Provider: claude-code
- Model: sonnet
- Total tokens: 275,256
- Input: 253,495
- Output: 21,761
- Est. cost: $0.00 (claude-code provider does not report per-call cost; subscription-billed via Claude Code CLI)

## Task count

- Requested: 25
- Generated: 35 (taskmaster over-generated by 10)
- Priority split: 18 high / 12 medium / 5 low
- Tasks with no dependencies: 2 (IDs 1, 7)
- Most-depended-on task: #3 (projects.yaml v2 schema) with 8 dependents

## Warnings observed

- stdout: `[WARN] Claude Code CLI not detected. Install it with: npm install -g @anthropic-ai/claude-code`
  - Taskmaster still produced output successfully despite the warning (it apparently fell back to SDK-direct path).

## Coverage analysis vs. PRD

### Covered (good)
- §6.1 pipeline.log → task #4
- §6.2 token tracking → task #5
- §6.3 cron failure/retry → task #6
- §6.4 SDK pinning + smoke test → task #7 ("Implement taskmaster SDK workaround smoke test"). Titled but labeled "medium" priority; PRD implies it is a blocker for anything using task-master SDK.
- §6.5 secrets (.env) → task #2
- §6.6 projects.yaml v2 → task #3
- §6.7 backup → task #8
- §6.8 ADR → task #9
- §6.9 add-project → task #10
- §6.10 needs-attention → task #11
- §6.11 shared skills library → task #12
- Module 1 (markdown renderer) → tasks #13–17 (5 tasks, good granularity)
- Module 2 (FastAPI) → tasks #18–24 (7 tasks, per-endpoint breakdown, good)
- Module 4 (Hermes integration) → tasks #25–28 (4 tasks, stubs only as PRD allows)

### GAPS — tasks NOT generated

1. **Module 3 (Next.js Frontend) — entirely missing.** PRD §Module 3 explicitly defines:
   - Dashboard UI with project list, project detail (kanban view), cross-project needs-attention view, blockers view, add task/fix form, embedded Hermes iframe
   - Phase 3a (localhost:3001) and Phase 3b (Vercel deploy)
   - Playwright E2E acceptance criteria
   - NOT ONE task in the generated 35 mentions Next.js, React, frontend, dashboard UI, or Vercel.
2. **§6.12 / §6.13 / §6.16 cross-cutting items** — matrix columns for these were not even in the titles survey; spot-check shows 4 cross-cutting tasks (#29 CI, #30 per-project .env, #31 unstale, #32 launchctl). PRD has 18 cross-cutting concerns — most collapsed or dropped.
3. **§6.18 code review / parallel-track evaluation** — mentioned in task #27 as "Document parallel-track evaluation framework" but no dedicated review-automation task.
4. **Playwright / E2E browser tests for frontend** — dropped with Module 3.
5. **Vercel deployment / env config** — dropped.
6. **`kira-add-project` skill (§Module 4 skills list)** — absent. Only `kira-hq-render-kanban`, `kira-hq-report` (stub), `kira-weekly-review` generated; `kira-add-project` as a skill (distinct from the CLI in task #10) is not scaffolded.
7. **Telegram command breakdowns** — rolled into one stub task (#25). Per-command (`/status`, `/blockers`, `/add`, `/fix`, `/review`) not individually scheduled.

### Observations on content fidelity
- No tasks reference the 4-module × 18-cross-cutting matrix from PRD §429. The structural "every module must implement each cross-cutting concern" requirement is lost — each cross-cutting task treats it as a one-off instead of per-module.
- Task dependencies look reasonable (1.8 avg, 33 blocked by deps, sensible DAG).
- Priority assignments generally sane; the SDK smoke test (§6.4) at "medium" is arguably miscategorized given PRD treats it as foundational.

## Deliverables in this directory

- `.taskmaster/tasks/tasks.json` — auto-generated by task-master (35 tasks)
- `tasks.json` — `task-master list --json` snapshot
- `tasks-list.txt` — `task-master list` human-readable output
- `parse-prd.stdout.log` — full stdout including telemetry panel
- `parse-prd.stderr.log` — spinner/success lines only
- `notes.md` — this file
