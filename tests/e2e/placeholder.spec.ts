import { test, expect } from "@playwright/test";

// Placeholder Playwright spec so `npx playwright test --list` succeeds
// before Modules 2/3 land. Replace with real flows in T-18 (Next.js DoD).
test("kira-hq dashboard placeholder", async () => {
  expect(1 + 1).toBe(2);
});
